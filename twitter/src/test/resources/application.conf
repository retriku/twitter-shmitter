test-database {
  driver = "slick.driver.H2Driver$"
  properties {
    connectionPool = disabled
    driver = "org.h2.Driver"
    url = "jdbc:h2:mem:tsql1;INIT=runscript from 'sql/create-schema.sql'"
  }
}